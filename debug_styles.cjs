const puppeteer = require("puppeteer"); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto("file:///C:/Users/Vikhyat Kaushik/AG Projects/Portfolio 26/bymonolog-clone-light/site/index.html", {waitUntil: "networkidle0"}); await new Promise(r => setTimeout(r, 2000)); const styledElements = await page.evaluate(() => { const els = Array.from(document.querySelectorAll("[style*=\"translate3d\"]")); return els.map(e => ({tag: e.tagName, cls: e.className})); }); console.log("Elements with translate3d:", styledElements.length); await browser.close(); })();
