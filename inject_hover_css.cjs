const fs = require("fs"); const css = `<style> .nav_wrap { transform: translateY(0%) !important; position: fixed !important; display: block !important; } .works_home_item:hover .reel_home_cover { opacity: 1 !important; visibility: visible !important; } .reel_home_cover { opacity: 0; transition: opacity 0.3s ease; pointer-events: none; z-index: 20; height: 75% !important; width: 75% !important; margin: auto; top: 0; bottom: 0; left: 0; right: 0; position: absolute; } .reel_home_video img { object-fit: contain !important; width: 100% !important; height: 100% !important; } </style>`; ["site/index.html", "site/play.html", "site/work.html"].forEach(file => { let html = fs.readFileSync(file, "utf8"); html = html.replace("</head>", css + "</head>"); fs.writeFileSync(file, html); }); console.log("Injected hover and nav CSS");
